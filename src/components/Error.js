import React from 'react'

class Error extends React.Component {
    render(){
        return (
            <h1>
                Some error with no page to return
            </h1>
        )
    }
}

export default Error;